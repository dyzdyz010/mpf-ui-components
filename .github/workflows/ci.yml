name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      sdk_version:
        description: 'SDK version (empty=latest)'
        required: false
        default: ''

env:
  QT_VERSION: '6.8.3'
  COMPONENT_NAME: 'mpf-ui-components'

jobs:
  resolve-versions:
    runs-on: ubuntu-latest
    outputs:
      sdk_version: ${{ steps.resolve.outputs.sdk_version }}
    steps:
      - name: Resolve dependency versions
        id: resolve
        run: |
          if [[ -n "${{ github.event.inputs.sdk_version }}" ]]; then
            SDK_VERSION="${{ github.event.inputs.sdk_version }}"
          else
            SDK_VERSION=$(gh release view -R dyzdyz010/mpf-sdk --json tagName -q '.tagName')
          fi
          echo "sdk_version=$SDK_VERSION" >> $GITHUB_OUTPUT
          echo "Using SDK: $SDK_VERSION"
        env:
          GH_TOKEN: ${{ github.token }}

  build:
    needs: resolve-versions
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            artifact_suffix: linux-x64
            arch: linux_gcc_64
            ext: tar.gz
          - os: windows-latest
            artifact_suffix: windows-x64
            arch: win64_mingw
            ext: zip

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          arch: ${{ matrix.arch }}
          cache: true
          cache-key-prefix: 'qt-${{ env.QT_VERSION }}'
          aqtversion: '==3.1.*'
          tools: ${{ runner.os == 'Windows' && 'tools_mingw1310' || '' }}

      - name: Setup MinGW (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: echo "$IQTA_TOOLS/mingw1310_64/bin" >> $GITHUB_PATH

      - name: Install deps (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y ninja-build libgl1-mesa-dev libxkbcommon-dev

      - name: Download SDK
        shell: bash
        run: |
          mkdir -p deps/mpf-sdk && cd deps/mpf-sdk
          gh release download ${{ needs.resolve-versions.outputs.sdk_version }} \
            -R dyzdyz010/mpf-sdk \
            -p "mpf-sdk-${{ matrix.artifact_suffix }}.${{ matrix.ext }}"
          [[ "${{ matrix.ext }}" == "tar.gz" ]] && tar -xzf *.tar.gz || 7z x *.zip
          rm -f *.${{ matrix.ext }}
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Configure (Linux)
        if: runner.os == 'Linux'
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_INSTALL_PREFIX=$PWD/install \
            -DCMAKE_PREFIX_PATH="$PWD/deps/mpf-sdk"

      - name: Configure (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          cmake -B build -G "MinGW Makefiles" \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_INSTALL_PREFIX="$PWD/install" \
            -DCMAKE_PREFIX_PATH="$PWD/deps/mpf-sdk"

      - name: Build
        shell: bash
        run: cmake --build build

      - name: Install
        shell: bash
        run: cmake --install build

      - name: Package (Linux)
        if: runner.os == 'Linux'
        run: cd install && tar -czvf ../${{ env.COMPONENT_NAME }}-${{ matrix.artifact_suffix }}.tar.gz .

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: cd install && 7z a -tzip ../${{ env.COMPONENT_NAME }}-${{ matrix.artifact_suffix }}.zip .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.COMPONENT_NAME }}-${{ matrix.artifact_suffix }}
          path: ${{ env.COMPONENT_NAME }}-${{ matrix.artifact_suffix }}.*

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: artifacts/*
          generate_release_notes: true

  # Cascade: ui-components -> host
  trigger-downstream:
    needs: release
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Trigger host
        run: |
          VERSION="${GITHUB_REF_NAME}"
          echo "Triggering host with ui-components version: $VERSION"
          gh workflow run ci.yml -R dyzdyz010/mpf-host -f ui_components_version=$VERSION
        env:
          GH_TOKEN: ${{ secrets.DISPATCH_TOKEN }}
